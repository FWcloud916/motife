# Dependencies
node_modules

# Remotion output
out
dist
build

# Remotion caches (also covered by node_modules, kept explicit for clarity)
.remotion

# Editor / OS
.DS_Store

# Nested git worktree checkouts created by agent tooling. Was only in
# .git/info/exclude, which is local to one machine and not shared by a clone.
.claude/worktrees/

# Env
.env
.env.*
!.env.example

# Logs
*.log

# Tooling caches
.eslintcache
*.tsbuildinfo

# Ignore the output video from Git but not videos you import into src/

# vitest coverage reports
coverage/
